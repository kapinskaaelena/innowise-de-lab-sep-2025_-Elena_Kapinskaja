
--  Шаг 1: Создание нового пользователя (роли) hr_user
-- ------------------------------------------------
-- CREATE ROLE — создаёт нового пользователя в PostgreSQL
-- LOGIN — разрешает вход в систему
-- PASSWORD — простой пароль для тестов 
-- ------------------------------------------------
CREATE ROLE hr_user WITH LOGIN PASSWORD 'hr123';

--  Шаг 2: Предоставление права SELECT на таблицу Employees
-- ------------------------------------------------
-- GRANT SELECT — разрешает читать данные из таблицы
-- Это как дать доступ "только посмотреть, но не трогать"
-- ------------------------------------------------
GRANT SELECT ON TABLE Employees TO hr_user;

-- 🔹 Шаг 3: Тест — подключение как hr_user и выполнение SELECT
-- ------------------------------------------------
-- В новой сессии  подключаемся как hr_user
-- Выполняем:
-- SELECT * FROM Employees;
--  Ожидаемый результат: данные выводятся — доступ на чтение работает
-- ------------------------------------------------

-- 🔹 Шаг 4: Тест — попытка выполнить INSERT как hr_user
-- ------------------------------------------------
-- Пробуем вставить нового сотрудника:
-- INSERT INTO Employees (FirstName, LastName, Department, Salary)
-- VALUES ('Test', 'User', 'HR', 50000.00);
-- ❌ Ожидаемый результат: ошибка доступа — прав на INSERT нет
-- ------------------------------------------------

-- 🔹 Шаг 5: Предоставление прав INSERT и UPDATE пользователю hr_user
-- ------------------------------------------------
-- GRANT INSERT, UPDATE — разрешаем добавлять и изменять данные
-- Это как дать ключи от шкафа, но не от сейфа — можно менять, но не удалять
-- ------------------------------------------------
GRANT INSERT, UPDATE ON TABLE Employees TO hr_user;

--  Шаг 6: Тест — выполнение INSERT и UPDATE как hr_user
-- ------------------------------------------------
-- В новой сессии выполняем:
-- INSERT INTO Employees (FirstName, LastName, Department, Salary)
-- VALUES ('Test', 'User', 'HR', 50000.00);
--
-- UPDATE Employees SET Salary = 51000.00 WHERE FirstName = 'Test' AND LastName = 'User';

